<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Forbidden Library - Arcane Archives</title>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="esoteric-bg"></div>
    
    <!-- Floating Esoteric Symbols -->
    <div class="floating-symbol" style="top: 10%; left: 5%;">☿</div>
    <div class="floating-symbol" style="top: 20%; left: 85%; animation-delay: -5s;">♀</div>
    <div class="floating-symbol" style="top: 60%; left: 10%; animation-delay: -10s;">♃</div>
    <div class="floating-symbol" style="top: 80%; left: 80%; animation-delay: -15s;">♄</div>
    
    <header>
        <div class="logo">ARCANE ARCHIVES</div>
        <nav>
            <ul>
                <li><a href="/" class="nav-link">Home</a></li>
                <li><a href="/library" class="nav-link active">Forbidden Library</a></li>
                <li><a href="/societies" class="nav-link">Secret Societies</a></li>
                <li><a href="/contact" class="nav-link">Contact</a></li>
                <li><a href="/upload-book" class="nav-link">Upload Books</a></li>
            </ul>
        </nav>
    </header>
    
    <main>
        <h1>The Forbidden Library</h1>
        <p>These texts have been suppressed, hidden, or declared dangerous by various authorities throughout history. Their contents reveal alternative histories, lost technologies, and spiritual practices deemed too powerful for the masses.</p>
        
        <!-- LEGENDARY FORBIDDEN TEXTS SECTION -->
        <h2>Legendary Forbidden Texts</h2>
        <p>These legendary texts await discovery. Upload their contents to unlock their secrets in the archives.</p>

        <div class="book-grid">
            <% if (typeof legendaryBooks !== 'undefined' && legendaryBooks.length > 0) { %>
               <% 
const legendaryImages = {
    'Emerald Tablet': '/images/books/emerald-tablet.jpg',
    'Voynich Manuscript': '/images/books/voynich-manuscript.jpg',
    'Book of Thoth': '/images/books/book-of-thoth.jpg',
    'Picatrix': '/images/books/picatrix.jpg',
    'Sworn Book of Honorius': '/images/books/sworn-book-honorius.jpg',
    'Ripley Scroll': '/images/books/ripley-scroll.jpg'
};
%>
                
                <% legendaryBooks.forEach((legend, index) => { %>
                    <% 
                    // Safe image URL with fallback
                    const imageUrl = legendaryImages[legend.title] || 'https://images.unsplash.com/photo-1589994965851-a8f479c573a9?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=60';
                    %>
                    <div class="book-card">
                        <div class="book-cover" style="background-image: url('<%= imageUrl %>')">
                            <!-- Icons removed from here -->
                        </div>
                        <div class="book-info">
                            <h3><%= legend.title %></h3>
                            <p><%= legend.description %></p>
                            <p class="book-meta"><strong>Attributed to:</strong> <%= legend.author %></p>
                            <p class="book-meta"><strong>Domain:</strong> <%= legend.category %></p>
                            
                            <% if (legend.isUploaded && legend.uploadedBookId) { %>
                                <div class="book-actions">
                                    <a href="/read/<%= legend.uploadedBookId %>" class="read-btn">📖 Read in Archives</a>
                                    <a href="/upload-book?title=<%= encodeURIComponent(legend.title) %>&author=<%= encodeURIComponent(legend.author) %>&category=<%= legend.category %>" 
                                       class="upload-btn">📤 Upload New Version</a>
                                </div>
                                <p style="color: var(--text-secondary); font-size: 0.8rem; margin-top: 0.5rem;">
                                    ✅ Available in Archives
                                </p>
                            <% } else { %>
                                <div class="book-actions">
                                    <a href="/upload-book?title=<%= encodeURIComponent(legend.title) %>&author=<%= encodeURIComponent(legend.author) %>&category=<%= legend.category %>" 
                                       class="upload-btn">📤 Upload Text</a>
                                </div>
                                <p style="color: #8b0000; font-size: 0.8rem; margin-top: 0.5rem;">
                                    🔒 Awaiting Discovery
                                </p>
                            <% } %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-books">
                    <p>Legendary texts information is currently unavailable.</p>
                </div>
            <% } %>
        </div>

        <!-- UPLOADED BOOKS SECTION -->
        <% if (books && books.length > 0) { %>
            <div class="uploaded-books">
                <h2>Archived Esoteric Texts</h2>
                <p>These forbidden texts have been uploaded to the archives and are available for study.</p>
                <% books.forEach(book => { %>
                    <div class="book-item">
                        <div class="book-info">
                            <h3><%= book.title %></h3>
                            <p class="book-meta">
                                <strong>Author:</strong> <%= book.author %> | 
                                <strong>Category:</strong> <%= book.category %> |
                                <strong>Uploaded:</strong> <%= book.uploadDate.toLocaleDateString() %>
                            </p>
                            <p><%= book.description %></p>
                            <p class="book-meta">
                                <strong>File:</strong> <%= book.originalName %> 
                                (<%= (book.fileSize / 1024 / 1024).toFixed(2) %> MB)
                            </p>
                        </div>
                        <div class="book-actions">
                            <a href="/read/<%= book._id %>" class="read-btn">Read</a>
                            <a href="<%= book.filePath %>" download="<%= book.originalName %>" class="download-btn">Download</a>
                            <form action="/upload/delete/<%= book._id %>" method="POST" style="display: inline;">
                                <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this forbidden knowledge?')">Delete</button>
                            </form>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-books">
                <h2>Archived Esoteric Texts</h2>
                <p>No forbidden texts have been uploaded yet. Be the first to contribute to the archives by uploading texts above.</p>
                <a href="/upload-book" class="upload-link">Upload First Text</a>
            </div>
        <% } %>
    </main>
    
    <footer>
        <p>Arcane Archives &copy; <span id="current-year"></span> | Knowledge Forbidden to the Uninitiated</p>
    </footer>

    <script src="/js/main.js"></script>
    
    <script>
        // Set current year
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('current-year').textContent = new Date().getFullYear();
        });
    </script>
</body>
</html>